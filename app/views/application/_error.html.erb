<% if object.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(object.errors.count, "error") %>: </h2>

    <% object.errors.full_messages.each do |message| %>
      <div class="notif notif-warning"><span><%= message %></span></div><br>
    <% end %>
  </div>
<% end %>